package name.dmitrym.theapp.routing

import org.specs2.mutable.Specification

class TestSessions extends Specification {
  "Sessions routing" should {
    "fail for invalid user" in {
      pending
    }
    "succeded for valid user" in {
      pending
    }
  }
}
